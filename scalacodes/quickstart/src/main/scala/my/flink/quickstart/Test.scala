package my.flink.quickstart

import Array._

//class Point(val xc: Int, val yc: Int) {
//  var x: Int = xc
//  var y: Int = yc
//
//  def move(dx: Int, dy: Int) {
//    x = x + dx
//    y = y + dy
//  }
//}

object Test {
  def main(args: Array[String]): Unit = {

//    val point = new Point(10, 20);
//    printPoint
//
//    def printPoint {
//      println("xxx" + point.x);
//      println("yyy" + point.y);
//    }

    //    val it = Iterator(1, 2, 5, 3)
    //    while (it.hasNext) println(it.next())
    //    println(it.max)
    //    println(it.size, it.length)
    var ppp = 1

    //    val x = List(1, 2, 3, 4)
    //    val y = Set(1, 3, 5, 7)
    //    val z = Map("one" -> 1, "two" -> 2, "three" -> 3)
    //    val w = (10, "runoob")
    //    val a: Option[Int] = Some(5)

    //    var myList1 = range(10, 20, 2)
    //    var my2 = range(10, 20)
    //    for (x <- myList1) {
    //      println(x)
    //    }
    //    for (x <- my2) {
    //      println(x)
    //    }
    //
    //    var res2 = Array.iterate(0, 3)(a => a + 2)
    //    var res1: Array[Int]
    //    = Array(0, 1, 2)

    //    var myList1 = Array(1.9, 2.9, 3.4, 3.5)
    //    var myList2 = Array(1.9, 2.9, 3.4, 3.5)
    //    var my3 = concat(myList1, myList2)
    //    for (x <- my3) {
    //      println(x)
    //    }


    //    var myMatrix = ofDim[Int](3, 3)
    //    for (i <- 0 to 2) {
    //      for (j <- 0 to 2) {
    //        myMatrix(i)(j) = j
    //      }
    //    }

    //    val a = "a"
    //    val b = "b"
    //    println(a.concat(b))
    //
    //    println("x".concat("y"))
    //
    //    var fs = printf("%f,%d,%s", 12.456, 2000, "rrr")
    //
    //    var z: Array[String] = new Array[String](3)
    //    var w = new Array[String](3)
    //    w = Array("1", "2")
    //
    //    var myList = Array(1.9, 2.9, 3.4, 3.5)
    //
    //    for (x <- myList) {
    //      println(x)
    //    }
    //
    //    var total = 0.0
    //    for (i <- 0 to (myList.length - 1)) {
    //      total += myList(i);
    //    }
    //
    //    var max = myList(0);
    //    for (i <- 1 until myList.length) {
    //      if (myList(i) > max)
    //        max = myList(i)
    //    }
    //
    //    println(total + " " + max)

    //    var buf = new StringBuilder;
    //    buf += 'a'
    //    buf += 'a'
    //    buf ++= "bcdef"
    //    println(buf.toString())
    //    println(buf.length())
    //    println(buf.length)
    //
    //    var x = 10
    //    if (x < 20) {
    //      println("x < 20")
    //    }
    //
    //    println(multi(1), multi(2))
  }

//  def m(x: Int) = x + 3
//
//  val f = (x: Int) => x + 3
//
//  def addInt(a: Int, b: Int): Int = {
//    var sum: Int = 0
//    sum = a + b
//    return sum
//  }
//
//  def printMe(): Unit = {
//    println("Hello Scala")
//  }
//
//  var factor = 3
//  val multi = (i: Int) => i * factor
//
//  var greeting = "Hello world";
//  var Gee: String = "hell";
}
